import { HighlightMousecdDirective } from './highlight-mousecd.directive';

describe('HighlightMousecdDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightMousecdDirective();
    expect(directive).toBeTruthy();
  });
});
